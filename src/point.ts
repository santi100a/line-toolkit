import assertTypeOf = require('@santi100a/assertion-lib/cjs/type-of')

/**
 * A point in 2D Cartesian space.
 * 
*/
interface Point {
    readonly x: number;
    readonly y: number;
}

throw new Error('Type-only module: do NOT import');

export = Point;